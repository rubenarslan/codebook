# Zap labelled class

Modelled on
[`haven::zap_labels()`](https://haven.tidyverse.org/reference/zap_labels.html),
zaps labelled class (not other attributes).

Modelled on
[`haven::zap_labels()`](https://haven.tidyverse.org/reference/zap_labels.html),
zaps labelled class (not other attributes).

## Usage

``` r
zap_labelled(x)

zap_labelled(x)
```

## Arguments

- x:

  the data frame or variable
